name: Server Keep-Alive

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render to prevent cold starts
        run: curl -fsS -o /dev/null https://api.loam.today/up
